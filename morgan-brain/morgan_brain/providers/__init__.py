"""Provider adapters, wire types, capability registry, router, and factory.

Nothing above this package should import a provider SDK directly — all SDK usage
is confined to ``morgan_brain.providers.adapters.*``.
"""
