"""Provider adapters.

All imports of provider SDKs (openai, httpx, etc.) are confined to this sub-package.
Nothing outside ``morgan_brain.providers.adapters`` should import SDK internals directly.
"""
