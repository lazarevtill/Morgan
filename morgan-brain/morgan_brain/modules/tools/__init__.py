"""Tools module — implements ``interfaces.ToolExecutor``.

Responsibility: register and run built-in tools behind the single PermissionGate.
Service: brain-api. Phase: 3.

Planned files: executor.py, builtin/{calculator,file_read,web_search,memory_search,fetch_url}.py.
"""
