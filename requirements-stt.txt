# STT service requirements
-r requirements-cuda.txt
# PyTorch versions inherited from requirements-cuda.txt (2.5.1+cu124)

# Core audio processing (faster-whisper with built-in VAD)
faster-whisper>=1.1.0
soundfile>=0.12.0
librosa>=0.10.0
pydub>=0.25.0
websockets>=14.0
aiofiles>=24.0

# Additional dependencies from requirements-base that STT needs
pyyaml>=6.0
aiohttp>=3.11
